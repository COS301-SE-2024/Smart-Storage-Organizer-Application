<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state (when the TextInputEditText is selected) -->
    <item android:state_focused="true" android:color="@color/colorPrimary"/>

    <!-- Default state (when not focused) -->
    <item android:state_focused="false" android:color="@android:color/white"/>

</selector>
