<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/color_card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="16dp"
    android:layout_margin="12dp"
    android:paddingVertical="32dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp">

        <!-- Container for centering text views -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_centerInParent="true">

            <!-- Name in the Middle -->
            <TextView
                android:id="@+id/color_code_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="DIGITAL DESIGN"
                android:textColor="@color/colorSecondary"
                android:layout_marginTop="16dp"
                android:textSize="16sp"/>

            <!-- Description in the Middle -->
            <TextView
                android:id="@+id/color_code_description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="100dp"
                android:layout_marginEnd="32dp"
                android:text="Creating Usability with Motion: The UX in Motion Manifesto"
                android:textColor="@color/colorSecondary"
                android:textSize="24sp"/>
        </LinearLayout>

        <!-- Arrow at the Bottom Right Corner -->
        <ImageView
            android:id="@+id/arrow"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/baseline_arrow_forward_24"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_gravity="bottom|end"/>
    </RelativeLayout>
</androidx.cardview.widget.CardView>
