<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="0dp" /> <!-- No rounded corners -->
    <solid android:color="@android:color/holo_red_dark" /> <!-- This color will be overridden by the `backgroundTint` in XML -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
